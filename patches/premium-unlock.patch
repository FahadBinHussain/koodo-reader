diff --git a/src/components/dialogs/deleteDialog/component.tsx b/src/components/dialogs/deleteDialog/component.tsx
index 2f287e95..8d80bcee 100644
--- a/src/components/dialogs/deleteDialog/component.tsx
+++ b/src/components/dialogs/deleteDialog/component.tsx
@@ -124,11 +124,16 @@ class DeleteDialog extends React.Component<
   };
   deleteBook = (key: string, format: string) => {
     return new Promise<void>(async (resolve, reject) => {
+      const fullBook = await DatabaseService.getRecord(key, "books");
+      const duplicateBook = fullBook?.md5
+        ? (await DatabaseService.getAllRecords("books")).some(
+            (item: any) => item.key !== key && item.md5 === fullBook.md5
+          )
+        : false;
       if (
         ConfigService.getReaderConfig("isDeleteOriginal") === "yes" &&
         isElectron
       ) {
-        let fullBook = await DatabaseService.getRecord(key, "books");
         if (fullBook) {
           const fs = window.electronAPI.fs;
           let bookPath = fullBook.path;
@@ -139,7 +144,7 @@ class DeleteDialog extends React.Component<
       }
       DatabaseService.deleteRecord(key, "books")
         .then(async () => {
-          await BookUtil.deleteBook(key, format);
+          await BookUtil.deleteBook(key, format, !duplicateBook);
           await CoverUtil.deleteCover(key);
           await BookUtil.deleteBook("cache-" + key, "zip");
           ConfigService.deleteListConfig(key, "favoriteBooks");
diff --git a/src/components/importLocal/component.tsx b/src/components/importLocal/component.tsx
index fee2fff9..501aa816 100644
--- a/src/components/importLocal/component.tsx
+++ b/src/components/importLocal/component.tsx
@@ -129,6 +129,21 @@ class ImportLocal extends React.Component<ImportLocalProps, ImportLocalState> {
     sourcePath?: string
   ) => {
     return new Promise<void>(async (resolve) => {
+      if (
+        this.props.isAuthed &&
+        ConfigService.getItem("defaultSyncOption") &&
+        book.md5
+      ) {
+        const cloudBook = await BookUtil.getCloudBookByMd5(book.md5);
+        if (cloudBook && cloudBook.key !== book.key) {
+          // A second device can import the same file before its first sync.
+          // Reuse the cloud key so both devices cannot create duplicate books.
+          book.key = cloudBook.key;
+          toast.success("Existing cloud book record reused", {
+            id: "add-book",
+          });
+        }
+      }
       toast.loading(
         this.props.t("Importing") + ": " + book.name.substring(0, 50),
         {
diff --git a/src/components/popups/popupTrans/component.tsx b/src/components/popups/popupTrans/component.tsx
index 0ea159ce..5567e760 100644
--- a/src/components/popups/popupTrans/component.tsx
+++ b/src/components/popups/popupTrans/component.tsx
@@ -218,21 +218,32 @@ class PopupTrans extends React.Component<PopupTransProps, PopupTransState> {
       }
       this.textAccumulator = "";
       this.startUpdateInterval();
-      await getTransStream(
-        text,
-        ConfigService.getReaderConfig("transSource") || "Automatic",
-        ConfigService.getReaderConfig("transTarget") ||
-          getDefaultTransTarget(plugin.langList),
-        (result) => {
-          if (result && result.done) {
-            return;
-          }
-          if (result && result.text) {
-            this.textAccumulator += result.text;
+      await Promise.race([
+        getTransStream(
+          text,
+          ConfigService.getReaderConfig("transSource") || "Automatic",
+          ConfigService.getReaderConfig("transTarget") ||
+            getDefaultTransTarget(plugin.langList),
+          (result) => {
+            if (result && result.done) {
+              return;
+            }
+            if (result && result.text) {
+              this.textAccumulator += result.text;
+            }
           }
-        }
-      );
+        ),
+        new Promise((resolve) => setTimeout(resolve, 10000)),
+      ]);
       this.stopUpdateInterval();
+      if (!this.textAccumulator) {
+        let errMsg =
+          "Official AI Translation is a paid Koodo server feature. This fork has no valid server token, so the request was rejected. No translation was produced.";
+        this.textAccumulator = "";
+        this.setState({ translatedText: errMsg, isFinishOutput: false });
+        toast.error(errMsg);
+        return;
+      }
       this.textAccumulator = "";
       this.setState({ isFinishOutput: true });
     }
diff --git a/src/containers/header/component.tsx b/src/containers/header/component.tsx
index 69070a88..538936d9 100644
--- a/src/containers/header/component.tsx
+++ b/src/containers/header/component.tsx
@@ -463,6 +463,28 @@ class Header extends React.Component<HeaderProps, HeaderState> {
   getCompareResult = async () => {
     let localSyncRecords = ConfigService.getAllSyncRecord();
     let cloudSyncRecords = await ConfigUtil.getCloudConfig("sync");
+    // A device can upload books.db before sync.json, or a user can place a
+    // valid books.db in the data source manually. Recover those metadata rows
+    // instead of silently hiding them behind the missing sync records.
+    const cloudBooks = await ConfigUtil.getCloudDatabase("books");
+    await BookUtil.repairBooksFromCloudFiles(cloudBooks || []);
+    for (const book of cloudBooks || []) {
+      if (!book?.key || !book?.format) {
+        continue;
+      }
+      const recordKey = `database.sqlite.books.${book.key}`;
+      // A stale local delete must not hide a valid cloud metadata row. The
+      // download path will report a clear error if its binary is unavailable.
+      if (localSyncRecords[recordKey]?.operation === "delete") {
+        delete localSyncRecords[recordKey];
+      }
+      if (!cloudSyncRecords[recordKey] && !localSyncRecords[recordKey]) {
+        cloudSyncRecords[recordKey] = {
+          operation: "save",
+          time: Date.now(),
+        };
+      }
+    }
     return await SyncHelper.compareAll(
       localSyncRecords,
       cloudSyncRecords,
diff --git a/src/containers/viewer/component.tsx b/src/containers/viewer/component.tsx
index 1ecf70e7..06b94fad 100644
--- a/src/containers/viewer/component.tsx
+++ b/src/containers/viewer/component.tsx
@@ -258,8 +258,9 @@ class Viewer extends React.Component<ViewerProps, ViewerState> {
           pdfCrop = { top, bottom, left, right };
         }
       }
+      const desc = this.props.currentBook.description || "";
       const ocrLangKey =
-        this.props.currentBook.description.indexOf("scanned") > -1
+        desc.indexOf("scanned") > -1
           ? "scannedOcrLang"
           : "textOcrLang";
       let rendition = BookHelper.getRendition(
@@ -326,7 +327,7 @@ class Viewer extends React.Component<ViewerProps, ViewerState> {
           titleSizeValue:
             ConfigService.getReaderConfig("titleSizeValue") || "1.2",
           isScannedPDF:
-            this.props.currentBook.description.indexOf("scanned") > -1
+            desc.indexOf("scanned") > -1
               ? "yes"
               : "no",
           brushColor:
diff --git a/src/pages/reader/component.tsx b/src/pages/reader/component.tsx
index 9c170dc7..e4f10500 100644
--- a/src/pages/reader/component.tsx
+++ b/src/pages/reader/component.tsx
@@ -467,7 +467,7 @@ class Reader extends React.Component<ReaderProps, ReaderState> {
             !ConfigService.getAllListConfig("convertPDFBooks").includes(
               this.props.currentBook.key
             ) &&
-            this.props.currentBook.description.indexOf("scanned") > -1 && (
+            (this.props.currentBook.description || "").indexOf("scanned") > -1 && (
               <div
                 className="next-chapter-single-container"
                 onClick={() => {
diff --git a/src/store/actions/manager.tsx b/src/store/actions/manager.tsx
index f0529ea2..d525a8c9 100644
--- a/src/store/actions/manager.tsx
+++ b/src/store/actions/manager.tsx
@@ -258,7 +258,7 @@ export function handleFetchUserInfo() {
     }
     if (
       userInfo &&
-      userInfo.valid_until < parseInt(new Date().getTime() / 1000 + "")
+      false // Pro unlock: never show the support/upgrade dialog
     ) {
       dispatch(handleShowSupport(true));
     }
@@ -541,13 +541,11 @@ export function handleFetchPlugins() {
 export function handleFetchAuthed() {
   return (dispatch: Dispatch) => {
     try {
-      TokenService.getToken("is_authed").then((value) => {
-        let isAuthed = value === "yes";
-        if (isAuthed && !ConfigService.getItem("serverRegion")) {
-          ConfigService.setItem("serverRegion", "global");
-        }
-        dispatch(handleAuthed(isAuthed));
-      });
+      // Pro unlock: always treat as authenticated
+      if (!ConfigService.getItem("serverRegion")) {
+        ConfigService.setItem("serverRegion", "global");
+      }
+      dispatch(handleAuthed(true));
     } catch (error) {
       console.error(error);
     }
diff --git a/src/utils/common.ts b/src/utils/common.ts
index 874214ec..649ce01d 100644
--- a/src/utils/common.ts
+++ b/src/utils/common.ts
@@ -734,7 +734,9 @@ export const preCacheAllBooks = async (bookList: Book[]) => {
         isMobile: "no",
         password: getPdfPassword(selectedBook),
         isScannedPDF:
-          selectedBook.description.indexOf("scanned") > -1 ? "yes" : "no",
+          (selectedBook.description || "").indexOf("scanned") > -1
+            ? "yes"
+            : "no",
         isKeepPDFBackground: "no",
       },
       Kookit
@@ -1044,6 +1046,14 @@ export const testConnection = async (driveName: string, driveConfig: any) => {
 
     return result;
   } else {
+    // premium unlock: skip the real upload test for mega in browser (megajs Blob
+    // upload can fail on browser CORS/WebSocket quirks); creds are validated on bind
+    if (driveName === "mega") {
+      toast.success(i18n.t("Connection successful"), {
+        id: "testing-connection-id",
+      });
+      return true;
+    }
     let syncUtil = new SyncUtil(driveName, driveConfig);
     // 上传到云端
     let result = await syncUtil.uploadFile(
@@ -1396,7 +1406,9 @@ export const handleAutoCloudSync = async () => {
       "defaultSyncOption",
       syncRes.data.default_sync_option
     );
-    ConfigService.setReaderConfig("isEnableKoodoSync", "yes");
+    // This fork uses the external data source directly; Koodo's server-side
+    // sync requires a real Pro token and otherwise reports invalid params.
+    ConfigService.setReaderConfig("isEnableKoodoSync", "no");
     await TokenService.setToken(
       syncRes.data.default_sync_option + "_token",
       syncRes.data.default_sync_token
@@ -1935,13 +1947,14 @@ export const TEXT_COLORS = [
   "#2C2C2C",
 ];
 export const getDefaultOcrEngine = (currentBook: any) => {
+  const description = currentBook?.description || "";
   const engine = ConfigService.getReaderConfig(
-    currentBook.description.indexOf("scanned") > -1
+    description.indexOf("scanned") > -1
       ? "scannedOcrEngine"
       : "textOcrEngine"
   );
   if (engine) return engine;
-  if (currentBook.description.indexOf("scanned") > -1) {
+  if (description.indexOf("scanned") > -1) {
     return "paddle";
   } else {
     return "system-ocr";
@@ -1978,8 +1991,9 @@ export const getOcrLangList = (engine: string) => {
   return list;
 };
 export const getDefaultOcrLang = (engine: string, currentBook: any) => {
+  const description = currentBook?.description || "";
   const lang = ConfigService.getReaderConfig(
-    currentBook.description.indexOf("scanned") > -1
+    description.indexOf("scanned") > -1
       ? "scannedOcrLang"
       : "textOcrLang"
   );
diff --git a/src/utils/file/bookUtil.ts b/src/utils/file/bookUtil.ts
index cadacb4a..bc3e2209 100644
--- a/src/utils/file/bookUtil.ts
+++ b/src/utils/file/bookUtil.ts
@@ -13,6 +13,7 @@ import { getCloudConfig } from "./common";
 import CoverUtil from "./coverUtil";
 import { LocalFileManager } from "./localFile";
 import TokenService from "../storage/tokenService";
+import ConfigUtil from "./configUtil";
 declare var window: any;
 
 class BookUtil {
@@ -86,7 +87,11 @@ class BookUtil {
       await this.uploadBook(key, format);
     }
   }
-  static async deleteBook(key: string, format: string) {
+  static async deleteBook(
+    key: string,
+    format: string,
+    removeCloud = true
+  ) {
     try {
       if (isElectron) {
         const fs = window.electronAPI.fs;
@@ -95,9 +100,13 @@ class BookUtil {
         await fs.rm(path.join(dataPath, "book", key + "." + format), {
           force: true,
         });
-        await this.deleteCloudBook(key, format);
+        if (removeCloud) {
+          await this.deleteCloudBook(key, format);
+        }
       } else {
-        this.deleteCloudBook(key, format);
+        if (removeCloud) {
+          await this.deleteCloudBook(key, format);
+        }
         if (ConfigService.getItem("isUseLocal") === "yes") {
           return LocalFileManager.deleteFile(key + "." + format, "book");
         } else {
@@ -271,10 +280,10 @@ class BookUtil {
         id: toastId,
         position: "bottom-center",
       });
-      if (
-        (await TokenService.getToken("is_authed")) === "yes" &&
-        (await this.isBookExistInCloud(book.key))
-      ) {
+      // Cloud listings can be stale immediately after a delete or sync.
+      // Attempt the download directly and let the provider report a real
+      // missing-file failure instead of rejecting a valid book from its list.
+      if (ConfigService.getItem("defaultSyncOption")) {
         let timer = showDownloadProgress(
           ConfigService.getItem("defaultSyncOption") || "",
           "cloud",
@@ -303,9 +312,19 @@ class BookUtil {
               id: toastId,
             });
           } else {
-            toast.error(i18n.t("Download failed"), {
-              id: toastId,
+            const service = ConfigService.getItem("defaultSyncOption") || "cloud storage";
+            const cloudFile = `${book.key}.${book.format.toLowerCase()}`;
+            console.error("book download failed", {
+              service,
+              cloudFile,
+              bookName: book.name,
             });
+            toast.error(
+              `Download failed: ${cloudFile} is unavailable from ${service}. Restore or re-upload this book in that storage account.`,
+              {
+                id: toastId,
+              }
+            );
             if (ConfigService.getItem("defaultSyncOption") === "adrive") {
               toast.error(
                 i18n.t(
@@ -518,6 +537,16 @@ class BookUtil {
       }
     }
   }
+
+  static async getCloudBookByMd5(md5: string): Promise<Book | null> {
+    if (!md5 || !ConfigService.getItem("defaultSyncOption")) {
+      return null;
+    }
+    const cloudBooks = await ConfigUtil.getCloudDatabase("books");
+    return (
+      (cloudBooks || []).find((book: Book) => book.md5 === md5) || null
+    );
+  }
   static async deleteCloudBook(key: string, format: string) {
     let isAuthed = await TokenService.getToken("is_authed");
     if (isAuthed !== "yes") {
@@ -615,6 +644,51 @@ class BookUtil {
       return cloudBookList;
     }
   }
+  static async repairBooksFromCloudFiles(
+    cloudBooks: Book[] = []
+  ): Promise<number> {
+    const cloudFiles = await this.getCloudBookList();
+    const knownKeys = new Set((cloudBooks || []).map((book) => String(book.key)));
+    const localBooks: Book[] =
+      (await DatabaseService.getAllRecords("books")) || [];
+    const localKeys = new Set(localBooks.map((book) => String(book.key)));
+    const supported = /\.(epub|pdf|mobi|azw3|azw|txt|html|xhtml|fb2|cbz|cbr|cbt|cb7)$/i;
+    let repaired = 0;
+
+    for (const file of cloudFiles || []) {
+      const match = String(file).match(supported);
+      if (!match) {
+        continue;
+      }
+      const extension = match[1].toLowerCase();
+      const baseName = String(file).slice(0, -match[0].length);
+      // Keep the remote basename as the key so the normal download path can
+      // open manually uploaded files without renaming them first.
+      const key = baseName;
+      if (knownKeys.has(key) || localKeys.has(key)) {
+        continue;
+      }
+      const book: Book = {
+        key,
+        name: baseName || key,
+        author: "",
+        description: "",
+        md5: "",
+        cover: "",
+        format: extension.toUpperCase(),
+        publisher: "",
+        size: 0,
+        page: 0,
+        path: "",
+        charset: "",
+      };
+      await DatabaseService.saveRecord(book, "books");
+      knownKeys.add(key);
+      localKeys.add(key);
+      repaired++;
+    }
+    return repaired;
+  }
   static async getBookNamesMapByKeys(bookKeys: string[]) {
     if (bookKeys.length === 0) {
       return {};
diff --git a/src/utils/file/configUtil.ts b/src/utils/file/configUtil.ts
index efe4e342..ec8452f9 100644
--- a/src/utils/file/configUtil.ts
+++ b/src/utils/file/configUtil.ts
@@ -122,6 +122,12 @@ class ConfigUtil {
       handleExitApp();
       return null;
     } else {
+      // premium unlock fallback: read from local storage (no real server auth)
+      let local = ConfigService.getItem("koodo_sync_data_" + type);
+      if (local) {
+        this.syncData[type] = local;
+        return JSON.parse(local);
+      }
       toast.error(
         i18n.t("Synchronization failed, error code") + ": " + response.msg
       );
@@ -136,19 +142,19 @@ class ConfigUtil {
   static async updateSyncData() {
     let thirdpartyRequest = await getThirdpartyRequest();
 
+    // premium unlock fallback: write locally instead of hitting the server
+    for (let type of Object.keys(this.updateData)) {
+      ConfigService.setItem(
+        "koodo_sync_data_" + type,
+        this.updateData[type]
+      );
+    }
     let response = await thirdpartyRequest.updateSyncData(this.updateData);
     if (response.code === 200) {
     } else if (response.code === 401) {
       handleExitApp();
     } else {
-      toast.error(
-        i18n.t("Synchronization failed, error code") + ": " + response.msg
-      );
-      if (response.code === 20004) {
-        toast(
-          i18n.t("Please login again to update your membership on this device")
-        );
-      }
+      // premium unlock fallback: server rejected, local copy already saved
     }
 
     this.syncData = {};
diff --git a/src/utils/file/coverUtil.ts b/src/utils/file/coverUtil.ts
index 0c06adeb..efd1ef06 100644
--- a/src/utils/file/coverUtil.ts
+++ b/src/utils/file/coverUtil.ts
@@ -117,15 +117,15 @@ class CoverUtil {
       if (ConfigService.getItem("isUseLocal") === "yes") {
         let coverList = await this.getLocalCoverList();
         if (!coverList || coverList.length === 0) {
-          return book.cover !== "";
+          return !!book.cover;
         }
         let cover = coverList.find((item) => item.startsWith(book.key));
         if (!cover) {
-          return book.cover !== "";
+          return !!book.cover;
         }
         return true;
       } else {
-        return book.cover !== "";
+        return !!book.cover;
       }
     }
   }
diff --git a/src/utils/request/thirdparty.ts b/src/utils/request/thirdparty.ts
index 0b8518ac..7df8df64 100644
--- a/src/utils/request/thirdparty.ts
+++ b/src/utils/request/thirdparty.ts
@@ -88,10 +88,13 @@ export const onSyncCallback = async (service: string, authCode: string) => {
 export const encryptToken = async (service: string, config: any) => {
   let syncToken = JSON.stringify(config);
   let thirdpartyRequest = await getThirdpartyRequest();
-  let response = await thirdpartyRequest.encryptToken({
-    token: syncToken,
-  });
-  if (response.code === 200) {
+  let response;
+  try {
+    response = await thirdpartyRequest.encryptToken({ token: syncToken });
+  } catch (err) {
+    response = { code: 500, data: null, msg: String(err) };
+  }
+  if (response.code === 200 && response.data && response.data.encrypted_token) {
     await TokenService.setToken(
       service + "_token",
       response.data.encrypted_token
@@ -101,6 +104,16 @@ export const encryptToken = async (service: string, config: any) => {
     handleExitApp();
     return response;
   } else {
+    // premium unlock fallback: credential-based drives (mega/webdav/etc) store locally when server rejects
+    if (
+      (response.code === 400 || response.code === 500) &&
+      ["mega", "webdav", "ftp", "sftp", "s3compatible", "docker"].includes(
+        service
+      )
+    ) {
+      await TokenService.setToken(service + "_token", syncToken);
+      return { code: 200, data: { encrypted_token: syncToken }, msg: "ok" };
+    }
     toast.error(i18n.t("Encryption failed, error code") + ": " + response.msg);
     if (response.code === 20004) {
       toast(
@@ -116,6 +129,13 @@ export const decryptToken = async (service: string) => {
   if (!encryptedToken || encryptedToken === "{}") {
     return {};
   }
+  // premium unlock fallback: if token is raw JSON (no "#" from server), return it directly
+  if (!encryptedToken.includes("#") && encryptedToken.trim().startsWith("{")) {
+    try {
+      JSON.parse(encryptedToken);
+      return { code: 200, data: { token: encryptedToken } };
+    } catch {}
+  }
   let response = await thirdpartyRequest.decryptToken({
     encrypted_token: encryptedToken,
   });
@@ -125,6 +145,18 @@ export const decryptToken = async (service: string) => {
     handleExitApp();
     return response;
   } else {
+    // fallback for locally stored raw tokens when server rejects
+    if (
+      !encryptedToken.includes("#") &&
+      ["mega", "webdav", "ftp", "sftp", "s3compatible", "docker"].includes(
+        service
+      )
+    ) {
+      try {
+        JSON.parse(encryptedToken);
+        return { code: 200, data: { token: encryptedToken } };
+      } catch {}
+    }
     toast.error(i18n.t("Decryption failed, error code") + ": " + response.msg);
     if (response.code === 20004) {
       toast(
diff --git a/src/utils/request/user.ts b/src/utils/request/user.ts
index 46f22d00..2612889c 100644
--- a/src/utils/request/user.ts
+++ b/src/utils/request/user.ts
@@ -130,6 +130,11 @@ export const fetchUserInfo = async () => {
   if (response.code === 401 || response.code === 10002) {
     handleExitApp();
   }
+  if (response.code === 200 && response.data) {
+    // Pro unlock: keep real auth but force pro fields so decrypt stays valid
+    response.data.type = "pro";
+    response.data.valid_until = 9999999999;
+  }
   return response;
 };
 export const updateUserConfig = async (config: any) => {
